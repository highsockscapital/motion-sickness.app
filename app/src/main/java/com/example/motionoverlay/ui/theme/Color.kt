package com.example.motionoverlay.ui.theme

import androidx.compose.ui.graphics.Color

// Material 3 Custom Palette — THEME & DESIGN SYSTEM
// #FFFFF0 = R=0xFF G=0xFF B=0xF0 => ARGB 0xFFFFFFF0
val WindowBackground = Color(0xFFFFFFF0) // Ivory / Off-white
val StatusBarColor = Color(0xFFFFFFF0) // matches window

val DarkCharcoal = Color(0xFF161610) // Text Primary & Main Icons
val TextPrimary = DarkCharcoal
val IconPrimary = DarkCharcoal
val StrokeBorder = DarkCharcoal
val Outline = DarkCharcoal

val CardSurface = Color(0xFFFFFFFF) // Card / Dialog #FFFFFF
val DialogBackground = Color(0xFFFFFFFF)

val PrimaryAccent = Color(0xFFFF9E43) // Soft Light Orange
val PrimaryAccentDark = Color(0xFFE68A2E)

val SurfaceVariantWarm = Color(0xFFF5F5E6) // Subtle Warm Surface #F5F5E6
val ItemCardBackground = SurfaceVariantWarm

val ButtonBase = Color(0xFFFFFFFF) // Buttons white base
val ButtonText = DarkCharcoal
val ButtonBorder = DarkCharcoal

// Legacy launcher blue retained for icon vector
val LauncherBlue = Color(0xFF0420F6)
